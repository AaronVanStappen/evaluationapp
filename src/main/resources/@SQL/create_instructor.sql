-- DROP TABLE `eval`.`instructor`;

CREATE TABLE `eval`.`instructor` (
  `id` BIGINT NOT NULL AUTO_INCREMENT,
  `firstname` VARCHAR(30) NOT NULL,
  `name` VARCHAR(30) NOT NULL,
  PRIMARY KEY (`id`));
